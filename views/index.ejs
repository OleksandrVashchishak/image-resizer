<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Image Resizer</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">
  <div class="container mt-5 mx-auto" style="max-width: 600px;">
    <h1 class="text-center mb-4">Image Resizer</h1>
    <form action="/resize" method="POST" enctype="multipart/form-data" class="border p-4 bg-white rounded">
      <div class="mb-3">
        <label for="images" class="form-label">Upload Image:</label>
        <input type="file" name="images" id="images" multiple class="form-control" required>
      </div>
      <div class="mb-3">
        <label for="newSize" class="form-label">Width (px):</label>
        <input type="number" name="newSize" id="newSize" class="form-control" required>
      </div>
      <button type="submit" class="btn btn-primary w-100">Process</button>
    </form>

    <% if (message) { %>
      <div class="alert alert-info mt-3"><%- message %></div>
    <% } %>

    <!-- Button to delete temporary files -->
    <form action="/delete-temp-files" method="POST" class="mt-3">
      <button type="submit" class="btn btn-danger w-100">Delete Temporary Files</button>
    </form>
  </div>
</body>
</html>
